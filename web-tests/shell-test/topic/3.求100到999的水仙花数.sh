#!/bin/bash

# 求一百到999的水仙花数目(各位的立方 + 十位的立方 + 百位的立方 = 他自身)

for((i=100;i<1000;i++))
do
    a=$[${i}/100] # 百位
    b=$[${i}%100/10] # 十位 
    c=$[${i}%10] # 个位
    if [ $[${a}*${a}*${a}+${b}*${b}*${b}+${c}*${c}*${c}] -eq ${i} ]
    then
        echo ${i}
    fi
done
